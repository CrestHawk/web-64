import chai = require("chai");

describe("test", () =>
{
    describe("When I run the tests", () =>
    {
        it("They should pass", () =>
        {
            chai.expect(true).to.equal(true);
        });
    });
});